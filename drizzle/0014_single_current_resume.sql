-- Historical resume revisions and their application/material references are
-- intentionally preserved. Current revision pointers are introduced and
-- backfilled by the append-only revision migration.
SELECT 1;
