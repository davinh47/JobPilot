import { getLocale, pick } from "@/lib/i18n";

export default async function PrivacyPage() {
  const locale = await getLocale();
  return <main className="legal-page"><a href="/login">← JobPilot</a><h1>{pick(locale, "隐私说明", "Privacy notice")}</h1><p>{pick(locale, "JobPilot Cloud 保存你主动提供的账户资料、简历、岗位、申请进度、AI 设置和生成材料，以提供求职管理功能。", "JobPilot Cloud stores account details, resumes, jobs, application history, AI settings, and generated materials that you provide so the service can operate.")}</p><h2>{pick(locale, "AI 服务", "AI providers")}</h2><p>{pick(locale, "开启 AI 功能时，完成任务所需的简历或岗位内容会发送给你选择的模型提供商。JobPilot 不会把姓名、电话或邮箱加入公开岗位搜索词。", "When AI is enabled, the resume or job content needed for the task is sent to the model provider you select. JobPilot does not add your name, phone number, or email address to public job-search queries.")}</p><h2>{pick(locale, "API Key 安全", "API key security")}</h2><p>{pick(locale, "Cloud 版 API Key 使用 AES-256-GCM 静态加密并按账户隔离，保存后不会回传到浏览器，也不会进入导出文件、提示词或公开搜索词。JobPilot 后端必须在执行 AI 请求时解密 Key，因此该设计不是零知识加密。建议使用专门为 JobPilot 创建并设置消费限制的 Key；你可以随时在设置中删除它。", "Cloud API keys are isolated per account and encrypted at rest with AES-256-GCM. After saving, keys are not returned to the browser or included in exports, prompts, or public search queries. The JobPilot backend must decrypt a key when executing an AI request, so this is not zero-knowledge encryption. Use a separate key with a provider spending limit; you can delete it from Settings at any time.")}</p><h2>{pick(locale, "你的控制权", "Your controls")}</h2><p>{pick(locale, "你可以下载原始简历，删除 API Key、简历和申请记录，也可以在设置中永久删除整个账户及其云端数据。", "You can download original resumes, delete API keys, resumes, and application records, or permanently delete the account and its cloud data from Settings.")}</p></main>;
}
